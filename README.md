To install data:
```
git clone git@github.com:preprocessed-connectomes-project/abide.git
cd abide
python download_abide_preproc.py -d rois_cc200 -p cpac -s filt_noglobal -o /path/to/abide-hggn/directory
```
